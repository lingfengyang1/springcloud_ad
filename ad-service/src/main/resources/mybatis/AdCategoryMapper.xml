<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ad.mapper.AdCategoryMapper">

    <resultMap id="BaseResultMap" type="com.example.ad.entity.AdCategory">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM ad_category ORDER BY sort ASC
    </select>

    <select id="findById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM ad_category WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.ad.entity.AdCategory">
        INSERT INTO ad_category (name, sort, create_time, update_time)
        VALUES (#{name}, #{sort}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.ad.entity.AdCategory">
        UPDATE ad_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM ad_category WHERE id = #{id}
    </delete>
</mapper>
