<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ad.mapper.AdMapper">

    <resultMap id="BaseResultMap" type="com.example.ad.entity.Ad">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="image_url" property="imageUrl"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM ad ORDER BY update_time DESC
    </select>

    <select id="findById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM ad WHERE id = #{id}
    </select>

    <select id="findByCategoryId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM ad WHERE category_id = #{categoryId} ORDER BY update_time DESC
    </select>

    <insert id="insert" parameterType="com.example.ad.entity.Ad">
        INSERT INTO ad (name, category_id, image_url, description, status, create_time, update_time)
        VALUES (#{name}, #{categoryId}, #{imageUrl}, #{description}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.ad.entity.Ad">
        UPDATE ad
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM ad WHERE id = #{id}
    </delete>
</mapper>
